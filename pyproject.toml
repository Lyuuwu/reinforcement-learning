[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cilab_rl"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*", "agents*", "experiments*", "envs*"]