[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cilab_RL"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*", "agents*", "envs*"]